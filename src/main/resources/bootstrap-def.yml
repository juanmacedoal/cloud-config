# --------------------------------------------
# MICROSERVICE NAME
# -------------------------------------------- 
spring:  
  application:
    name: cloud-config
    
# --------------------------------------------
# SPRING CLOUD CONFIGURATION
# --------------------------------------------     
  cloud: 
    service-registry:
      auto-registration:
        fail-fast: false
    bus:
      enabled: false
    config:
      enabled: false
      allow-override: false
      server:
        git:
          uri: ${GITCONFIG_URI:https://git-codecommit.eu-west-1.amazonaws.com/v1/repos/cloud-configurations}     
          username: ${GITCONFIG_USERNAME:support@cashlight.io-at-156104699665}
          password: ${GITCONFIG_PASSWORD:UOauvpdNEM/qbKUXJVqZpAy0VtKR24vYwF7shGvVC9I=}     
          default-label: ${GITCONFIG_LABEL:master}                                
          timeout: 60
          search-paths: 
            - back/def
            - front     
            - front/workstation
            - front/console   
            - front/simulator/cards    
          
# --------------------------------------------
# EUREKA CONFIGURATION
# --------------------------------------------   
eureka:
  instance:
    prefer-ip-address: false 
    lease-renewal-interval-in-seconds: 30
  client: 
    serviceUrl:
      defaultZone: ${DISCOVERY_URI:http://localhost:8082/eureka}
    enabled: false
    register-with-eureka: false
    fetch-registry: false
  